{
  "name": "@envyron/db",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "db:dev:up": "docker compose --env-file .env -f ../../docker/dev/docker-compose.service.yml up -d",
    "db:dev:down": "docker compose --env-file .env -f ../../docker/dev/docker-compose.service.yml down -v",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.15.5",
    "drizzle-kit": "^0.31.5",
    "tsx": "^4.20.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.6",
    "pg": "^8.16.3"
  },
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  }
}
